<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RumarApp.Views.Layout.LayoutControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RumarApp.Views.Layout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <NavigationView x:Name="sideNav" SelectionChanged="sideNav_SelectionChanged"
                    PaneDisplayMode="Left"
                    IsBackButtonVisible="Collapsed"
                    Grid.ColumnSpan="2">

            <NavigationView.MenuItems>
                <NavigationViewItem Style="{StaticResource SidebarButton}" Icon="Home" Content="Dashboard" Tag="dash" />
                <NavigationViewItem Style="{StaticResource SidebarButton}" Icon="Page2" Content="Préstamos" Tag="loan" />
                <NavigationViewItem Style="{StaticResource SidebarButton}" Icon="Refresh" Content="Menu Item3" Tag="SamplePage3" />
                <NavigationViewItem Style="{StaticResource SidebarButton}" Icon="Download" Content="Menu Item4" Tag="SamplePage4" />
            </NavigationView.MenuItems>

            <!-- This is the right place to add the main frame -->
            <Frame x:Name="MainFrame" />
        </NavigationView>
    </Grid>
</UserControl>
