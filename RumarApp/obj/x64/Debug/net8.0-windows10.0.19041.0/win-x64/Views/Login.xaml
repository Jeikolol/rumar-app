<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RumarApp.Views.LoginControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RumarApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="20">
        <Grid Padding="20">
            <StackPanel Grid.Row="0" Spacing="20" Margin="0,40,0,40" HorizontalAlignment="Center">
                <Image Source="/Assets/logo2.png" Width="320" Height="150"/>
                <TextBlock Text="Inicia sesión en tu cuenta" 
                           FontSize="16" 
                           Foreground="{StaticResource Gold300Brush}"
                           HorizontalAlignment="Center"/>

                <TextBox x:ConnectionId='2' x:Name="EmailBox"
                         PlaceholderText="Correo electrónico" 
                         Foreground="{StaticResource Gold300Brush}" 
                                                           />
                <!-- Email Validation -->
                <StackPanel x:ConnectionId='3' x:Name="InvalidEmailError" Orientation="Horizontal" Spacing="4">
                    <FontIcon Glyph="&#xEA39;" Foreground="{ThemeResource SystemFillColorCriticalBrush}" FontSize="14"/>
                    <TextBlock Text="Correo electrónico inválido" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                </StackPanel>

                <PasswordBox x:ConnectionId='4' x:Name="PasswordBox" Margin="0,10,0,0" Foreground="{StaticResource Gold300Brush}"/>
                <Button x:ConnectionId='5' Content="Iniciar sesión"                     Margin="0,10,0,0" Style="{StaticResource GoldenButtonStyle}" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>

